<Page
    x:Class="CatMouse.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CatMouse"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="grdRoot"  Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid x:Name="grdBoard" Background="Black" Height="600" Width="600" BorderThickness="1" BorderBrush="Black">
            <Grid.RowDefinitions>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
            </Grid.ColumnDefinitions>
        </Grid>
        <TextBlock x:Name="catTblCount" FontWeight="Bold" Height="35" Foreground="OrangeRed" HorizontalAlignment="Left" VerticalAlignment="Top"/>
        <TextBlock x:Name="mouseTblCount" FontWeight="Bold" Height="35" Foreground="Aqua" HorizontalAlignment="Left" VerticalAlignment="Bottom"/>
    </Grid>
</Page>
